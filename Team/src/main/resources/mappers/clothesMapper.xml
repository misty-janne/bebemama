<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="c.l.o">

	<select id="selectlist" resultType="ClothesDTO">
		select * from clothes
	</select>

	<select id="getGoodsData" resultType="ClothesDTO">
		select * from clothes where gno = #{gno}
	</select>
	
</mapper>

